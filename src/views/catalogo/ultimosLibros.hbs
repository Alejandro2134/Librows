<div class="container p-4">
    <h2>Ultimos libros agregados:</h2>
    <br>
    <div class="row">
        {{#each newBooks}}
            <div class="col-md-3">
                <div class="card text-center">
                    <img src="{{imagen}}" class="card-img-top" height="370" width="500">
                    <div class="card-body">
                       <a href="/catalogo/{{idLibro}}" class="btn btn-primary">Ver libro</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>